[module/updates]
type = custom/script
exec = $HOME/.config/polybar/scripts/check-updates.sh

format-prefix = "%{T5} %{T-}"
format-prefix-foreground = ${colors.foreground-alt}

tail = false
interval = 120
